MSFS Hangar Release 238 Fix

Changes in this rebuild:
- Virtual Pilot route now shows the new Integrated Virtual Pilot Engine screen instead of the legacy map-first VP page.
- Sidebar entry 'Flight' removed so only the integrated Virtual Pilot workflow is exposed.
- Added native-panel state reporting on the Virtual Pilot screen so it is obvious whether the desktop browser pane opened.
- Added stronger engine status cards and log tail visibility on the Virtual Pilot screen.
- Virtual Pilot now auto-requests the native browser panel when the integrated VP engine becomes ready in Qt desktop mode.
- Returning from detail pages opened from Virtual Pilot now returns to Virtual Pilot, not the removed Flight screen.
