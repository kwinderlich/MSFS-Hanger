# MSFS Hangar Release 166 Notes

- Fixed Explorer Globe marker/layer refresh using latest state refs so markers and ICAO labels should persist on first open instead of flashing or disappearing.
- Range Aircraft now searches across the full library, not just the current geo-filtered map items, and choosing a suggestion applies by aircraft id.
- Region jumps now target roughly 1200 km altitude (about 648 NM) for non-world regions.
