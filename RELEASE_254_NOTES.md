# Release 254

- Based on Release 251 rollback baseline.
- Pause Sim in the integrated VP map now routes through /api/pomax/command instead of the legacy /api/flight/command SimConnect stub.
- Pomax runtime sync/build tag bumped to 254.
