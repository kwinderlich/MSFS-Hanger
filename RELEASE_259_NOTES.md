# Release 259

- Added optional Slew to WP1 behind a checkbox, implemented in the Hangar wrapper layer only.
- No changes to Pomax plane.js; integration-sensitive browser client code left untouched.
- ALT with AP remains ON by default.
